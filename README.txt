
argMaxSpec.py

    contains functions:

    def plotSpecArgMax(waveform, rate, N, hop_size) :
    return arg_max

    def plotSpecArgMax(waveform, rate, N, hop_size) :
    return arg_max

computeBsplineVal.py

    contains functions:

    def computeSplineVal(d, k, c, t) :
    return fval

    def newBsplineVal(d, k, j, t) :
    return fval

cycle2waveform.py

    def getBcoeffs(waveform, cycle, n) :
    return bcoeffs

cycleBcoeffs.py

    def getBcoeffs(waveform, cycle, n) :
    return bcoeffs

cycleSpline.py

    def plotCycleSpline(waveform, cycle_num, a, b, n) :

findCycles.py

    def getCycles(waveform, rate, freq) :
    return cycles

plotSpec.py

    def _plot(waveform, sample_rate, title):

    def plot_waveform(waveform, sample_rate):

    def plot_specgram(waveform, sample_rate):

rec2spec.py

    (no functions yet)

recSpec.py

    (no functions yet)
    
record.py

    (no functions yet)
    
test.py

    plotCycleSpline(waveform, a, b, n)

torchSpline.py

    (no functions yet)

wavspline.py

    (no functions yet)
